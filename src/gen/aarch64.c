#include <assert.h>
#include <stdio.h>

#include "ir.h"
#include "gen.h"

void codegen_aarch64_run(IR_CTX *ctx, FILE *output)
{
	(void) ctx;
	(void) output;

	assert(0 && "TODO not implemented codegen_aarch64_run");
}
